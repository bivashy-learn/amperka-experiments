#define LED_PIN 13

void setup() {
  Serial.begin(9600);
  pinMode(LED_PIN, OUTPUT);
}

void loop() {
  int read = Serial.read();
  if (read == '1') {
    digitalWrite(LED_PIN, HIGH);
  } else if (read == '0') {
    digitalWrite(LED_PIN, LOW);
  }
}
